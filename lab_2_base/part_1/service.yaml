apiVersion: v1
kind: Service
metadata:
  name: hello-service
  namespace: lab2
  labels:
    app: hello-app
    app.kubernetes.io/part-of: lab-2-base
spec:
  type: NodePort
  selector:
    app: hello-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
